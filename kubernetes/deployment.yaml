apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-links-profile-main-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-links-profile-main-app
  template:
    metadata:
      labels:
        app: social-links-profile-main-app
    spec:
      containers:
        - name: social-links-profile-main-app
          image: social-links-profile-main-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
